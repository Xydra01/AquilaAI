[pytest]
testpaths = tests
pythonpath = .
qt_api = pyside6
markers =
    live: tests that require Ollama (OLLAMA_BASE_URL, default http://127.0.0.1:11434)
    heretic: live tests targeting OLLAMA_MODEL=aquila_heretic (see test_live_heretic_tools.py)
    gui: tests that require PySide6 / pytest-qt
